<Window x:Class="RailwayMonitorClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:wf="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms"
        xmlns:wfh="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RailwayMonitorClient"
        xmlns:views="clr-namespace:RailwayMonitorClient.Views"
        mc:Ignorable="d"
        WindowState="Maximized"
        Title="监控" Height="1080" Width="1920"
        Closed="MainWindow_Closed">
    <Grid x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 导航栏 -->
        <views:AutoHideNavigationBar Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="4"
                                       x:Name="NavigationBar"
                                       NavigationRequested="NavigationBar_NavigationRequested"/>

        <!-- 使用代码动态创建摄像头控件 -->
    </Grid>
    
</Window>
